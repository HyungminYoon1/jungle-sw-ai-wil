# Week 8 - 핵심 역량 목표 달성률 평가

> 📅 작성 시점: 주차 마무리 시점

한 주가 마무리되는 시점에 목표에 대한 달성율을 핵심 역량별로 기입합니다.

---

## 1. 문제해결
- **목표:** 핵심 주제 8개(`BSD socket`, `IP/TCP/HTTP`, `file descriptor`, `DNS`, `echo/client-server`, `tiny 웹서버`, `proxy 기본 흐름`, `미니 DBMS API 서버`)를 학습하고 적용해 총 **8개 이상 문제 해결**
- **달성률:** 100% (8 / 8)
- **회고:** 네트워크 기본 개념부터 echo 서버, tiny 웹서버, proxy, 미니 DBMS API 서버까지 이번 주 핵심 주제를 모두 학습하고 구현 흐름에 적용했다.


## 2. 설계
- **목표:** 수요 코딩회 API 서버 구현 시 **스레드 풀 구조, 요청 처리 흐름, DB 엔진 연결 방식, 동시성 대응 포인트, 테스트 방식** 등 설계 포인트 **5개 이상**을 README 또는 설계 메모에 명시
- **달성률:** 100% (5 / 5)
- **회고:** API 서버에서 `accept loop -> bounded queue -> worker thread pool` 요청 분배 구조, HTTP 요청 처리 흐름, 기존 SQL 엔진 연결 방식, 테이블 단위 락과 인덱스 레지스트리 보호 전략, 자동 테스트 기준을 정리해 설계 목표를 충족했다.


## 3. 구현
- **목표:** 웹/프록시 서버 핵심 구현 5개(`socket 기반 요청/응답`, `echo/client-server`, `tiny 웹서버`, `homework 3문제 이상`, `proxy 과제 도전`) + API 서버 핵심 구현 4개(`외부 API 요청 처리`, `스레드 풀`, `병렬 SQL 처리`, `DB 엔진 연동`) = **9개 이상 구현 완료**
- **달성률:** 100% (9 / 9)
- **회고:** Web/Proxy Lab에서는 socket 기반 echo client/server, tiny 웹서버, 숙제 풀이, proxy의 기본 전달 흐름과 병렬 처리·캐시 확장을 구현했다. API 서버에서는 `GET /`, `GET /health`, `POST /query`, worker thread pool, bounded queue, 병렬 SQL 처리, 기존 SQL 처리기와 B+ Tree 인덱스 연동을 구현했다.


## 4. 품질
- **목표:** tiny 웹서버 동작 확인 1개 + homework 풀이 검토 1개 + API 서버 단위 테스트 2개 + API 기능 테스트 2개 + README 데모 점검 1개 = **7개 이상 검증 항목 통과**
- **달성률:** 100% (7 / 7)
- **회고:** tiny 웹서버와 숙제 풀이를 직접 확인했고, API 서버는 요청 파싱, 라우팅, 큐, 락, 엔진 연동, 정상/오류/경계값/동시성 시나리오를 자동 테스트로 검증했다. 자동 테스트 기준 `Tests run: 387`, `Tests failed: 0` 결과를 확인했다.


## 5. 유지보수
- **목표:** 이번 주(금~목) **7일 중 5일 이상** 개인 또는 팀 레포지토리에 의미 있는 커밋 메시지로 웹/프록시 서버, API 서버, 테스트, README 작업을 커밋·푸시
- **달성률:** 100% (5 / 5)
- **회고:** 2026-04-17, 2026-04-18, 2026-04-20, 2026-04-21, 2026-04-22에 week8 계획, 체크리스트, 팀 협업, 학습 문서, 퀴즈 리뷰를 의미 단위로 커밋해 주간 유지보수 목표를 달성했다.


## 6. 협업
- **목표:** 팀 협업 룰 정리(1개) + 웹/프록시 서버 학습 공유(1개) + API 서버 팀 프로젝트 참여(1개) = **3개 완료**
- **달성률:** 100% (3 / 3)
- **회고:** 팀 협업 룰을 정리했고, BSD socket, TCP/IP, HTTP, tiny, proxy 학습 흐름을 팀 스터디에서 공유했다. 수요 코딩회에서는 API 서버 요구사항 분석, 동시성 설계, 테스트 보완, README 정리에 참여했다.


## 7. 태도
- **목표:** 공통 6개(목표 수립, 달성률 평가, AI 원칙, 업무 검토, WIL, 팀 협업 룰) 중 **6개 완료**
- **달성률:** 100% (6 / 6)
- **회고:** 목표 수립, AI 활용 원칙, 업무 검토, 팀 협업 룰, 달성률 평가, WIL 작성 흐름을 주차 마무리 기준으로 정리했다.


## 8. 비즈니스 이해
- **목표:** 이번 주 API 서버와 프록시 서버가 **왜 필요한지**, 그리고 외부 요청 처리와 내부 DB 엔진 연결이 어떤 의미를 가지는지 README에 **2문장 이상** 명시
- **달성률:** 100% (2 / 2)
- **회고:** proxy는 클라이언트와 원 서버 사이에서 요청을 중계하고 캐시·병렬 처리 같은 확장 지점을 제공한다는 점을 정리했다. API 서버는 외부 클라이언트가 기존 DB 엔진 기능을 네트워크 요청으로 사용할 수 있게 하는 경계 계층이며, 내부 SQL 처리기와 B+ Tree 인덱스를 재사용하는 구조임을 설명했다.


## 9. AI 활용
- **목표:** AI 활용 원칙 수립(1개) + 웹/프록시 또는 API 서버 핵심 로직 검증·원리 설명(1개) = **2개 완료**
- **달성률:** 100% (2 / 2)
- **회고:** AI 활용 원칙에서 학습 과제는 보조 수준으로 제한하고, 수요 코딩회는 적극 활용하되 핵심 구조를 설명할 수 있어야 한다는 기준을 세웠다. proxy 요청 전달, 캐시, thread pool, table-level lock, DB 엔진 adapter 흐름을 검증하고 설명 가능한 수준으로 정리했다.


## 10. 학습 민첩성
- **목표:** `BSD socket`, `IP`, `TCP`, `HTTP`, `file descriptor`, `DNS`, `tiny`, `proxy`, `thread pool`, `동시성 이슈`, `API 서버 아키텍처`를 포함해 **11개 이상 개념/구현 포인트**를 학습 후 적용
- **달성률:** 100% (11 / 11)
- **회고:** 네트워크 기초 개념을 echo와 tiny 구현으로 연결했고, proxy에서 요청 전달·병렬 처리·캐시를 확장했다. 이후 API 서버에서 thread pool, bounded queue, table-level lock, 기존 DB 엔진 adapter 구조까지 적용해 학습 내용을 실제 구현으로 이어갔다.


---

### 📊 주간 요약

| # | 역량 | 달성률 |
|---|------|--------|
| 1 | 문제해결 | 100% |
| 2 | 설계 | 100% |
| 3 | 구현 | 100% |
| 4 | 품질 | 100% |
| 5 | 유지보수 | 100% |
| 6 | 협업 | 100% |
| 7 | 태도 | 100% |
| 8 | 비즈니스 이해 | 100% |
| 9 | AI 활용 | 100% |
| 10 | 학습 민첩성 | 100% |
| | **평균** | **100%** |

- **상위 역량:** 문제해결, 설계, 구현, 품질, 유지보수, 협업, 태도, 비즈니스 이해, AI 활용, 학습 민첩성
- **개선 필요:** 없음
